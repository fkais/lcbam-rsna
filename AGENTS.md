# LCBAM-RSNA 开发规则

## Project Context Loading

开始任何项目任务前，按以下顺序读取：

1. `AGENTS.md`
2. `PROJECT_CONTEXT.md`
3. `EXPERIMENT_PROTOCOL.md`
4. `PROJECT_LOG.md`

然后检查当前仓库和相关源码再作决定，不把旧聊天记录当作事实来源。若文档与当前代码或已核验实验产物冲突：先核实真实仓库状态，采用已核验事实，更新过时文档，并在 `PROJECT_LOG.md` 记录纠正。

不得推断未经验证的实验结果，不得把 smoke 指标当作正式性能结果。

## 项目目标

本仓库用于复现并扩展基于 YOLOv8n 的 RSNA Pneumonia Detection 实验：

1. 复现 YOLOv8n baseline、YOLOv8n + CBAM、YOLOv8n + 原始 LCBAM。
2. 独立开发和评估改进后的 `LCBAMv2`。

最终仓库应当可复现、结构清晰，并适合公开发布。

## 工程原则

- 不得手工修改 Python `site-packages` 中的文件。
- 自定义注意力模块必须保存在本仓库中。
- 不得静默更改历史实验实现。
- 必须保留原始 `LCBAM`，以便复现实验。
- 新模块统一命名为 `LCBAMv2`，并与原始 `LCBAM` 分开实现。
- 优先采用最小、可维护的改动。
- 未通过轻量验证前，不得启动长时间训练任务。
- 实验配置必须显式且可复现。
- 必须记录重要随机种子、模型配置、图像尺寸、batch size、epochs、依赖版本和数据集配置。
- 不得提交数据集、模型权重、大型生成文件、密钥或环境相关文件。
- 除非任务明确要求，不得修改 `archive/legacy_experiments/`。

## 训练前验证

开始完整实验前，必须依次确认：

1. 导入正常。
2. 模型可以构建。
3. 最小前向传播通过。
4. 相关单元测试通过。
5. 正确的注意力模块确实插入模型。
6. 设备和依赖版本正确。
7. 上述检查全部通过后，才可开始长时间训练。

## 项目文档

`PROJECT_LOG.md` 是项目的持续开发记录。

开始任务前：

- 先读取 `PROJECT_LOG.md`。
- 判断项目当前实际状态。
- 避免重复已经完成的工作。

完成每个有意义的任务后：

- 向 `PROJECT_LOG.md` 追加日志，并更新顶部当前状态。
- 除非纠正明确错误，不得删除或改写历史日志。
- 如果任务失败，必须记录失败及原因，不能将其写成成功。

每条日志至少包括：

- 日期和时间
- 任务与目标
- 修改前状态
- 修改内容和文件
- 执行的命令或测试
- 实际结果
- 发现的问题或风险
- 作出的决定
- 下一项建议任务

## 完成定义

只有同时满足以下条件，任务才算完成：

- 请求的变更已经实现；
- 已执行与风险相称的测试或检查；
- 结果得到清晰汇报；
- `PROJECT_LOG.md` 已更新。
